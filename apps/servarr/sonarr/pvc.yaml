---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sonarr-local-config-pvc
  labels:
    app: sonarr
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: local-path
