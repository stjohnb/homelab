---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: overseerr-local-config-pvc
  labels:
    app: overseerr
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: local-path
