apiVersion: v1
kind: Service
metadata:
  name: seerr
  labels:
    app: seerr
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 5055
      targetPort: 5055
      protocol: TCP
  selector:
    app: seerr
