apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: awtrix
  namespace: default
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
spec:
  ingressClassName: traefik-traefik
  tls:
  - hosts:
    - awtrix.home.bstjohn.net
    secretName: wildcard-home-tls
  rules:
  - host: awtrix.home.bstjohn.net
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: awtrix
            port:
              number: 80
