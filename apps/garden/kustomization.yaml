apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - pvc-photos.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml

# NOTE: `garden-db-secret` is NOT in Git. It is created by
# migrations/0028-garden-db.sh, which runs automatically on Flux reconcile.
# Never declare it as a manifest resource — Flux SSA would reset .data every
# reconcile and wipe the generated password.
